import React from 'react';

function AlarmOverride() {


    return (
        <div className={`alarm_override`}>
            Alarm override is active.
        </div>
    );
}

export default AlarmOverride;
